@model IEnumerable<MedicalTests.Domain.Categories>

@{
    ViewBag.Title = "Специальности";
}

<h2>Специальности</h2>
<body>
</body>
@using (Html.BeginForm("Index", "Categories", FormMethod.Post))
{
    <button type="submit" name="action" value="ClearTable">Очистить таблицу</button>
    <p>
    </p>
}
<p>
    @Html.ActionLink("Создать специальность", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameCategory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameCategory)
        </td>
        <td style="color:black">
            @Html.ActionLink("Редактировать", "Edit", new { id=item.ID_Category }) |
            @Html.ActionLink("Подробно", "Details", new { id=item.ID_Category }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.ID_Category })
        </td>
    </tr>
}

</table>
