@model IEnumerable<MedicalTests.Domain.Razrabotchiki>

@{
    ViewBag.Title = "Врачи";
}

<h2>Врачи</h2>
<body>
</body>
@using (Html.BeginForm("Index", "Razrabotchiki", FormMethod.Post))
{
    <button type="submit" name="action" value="ClearTable">Очистить таблицу</button>
    <button type="submit" name="action" value="LoadTable">Заполнить таблицу</button>
    <p>
    </p>
}
<p>
    @Html.ActionLink("Добавть врача", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Surname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Thirdname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateBirth)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Thirdname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateBirth)
            </td>
            <td>
                @Html.ActionLink("Редактировать", "Edit", new { id = item.ID_Razrabotchik }) |
                @Html.ActionLink("Подробно", "Details", new { id = item.ID_Razrabotchik }) |
                @Html.ActionLink("Удалить", "Delete", new { id = item.ID_Razrabotchik })
            </td>
        </tr>
    }

</table>
